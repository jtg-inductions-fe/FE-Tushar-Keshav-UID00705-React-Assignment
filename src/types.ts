export type ErrorDetails = {
    imgSrc: string;
    title: string;
    subtext: string;
    buttonText: string;
};

export type ResponsiveWidth = {
    mobile: string;
    tablet?: string;
    smallDesktop?: string;
    desktop?: string;
    largeDesktop?: string;
};
