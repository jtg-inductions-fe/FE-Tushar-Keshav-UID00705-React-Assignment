import { theme } from '@theme';

export const HEADER_HEIGHT = theme.typography.pxToRem(72);
export const DROP_SHADOW = `drop-shadow(0 ${theme.typography.pxToRem(6)} ${theme.typography.pxToRem(6)} rgba(0,0,0,0.25))`;
